default:

  Fact:

    force   : true
    metas   : [ metas, params, dependOn, mustDepend, force, stateMachine, indexKeys, conditions ]

  Path:

    params        : [ path, followSymlinks ]
    indexKeys     : path
    conditions    : TidBits::Facts::Conditions::Path
    exist         : true

    # Whether to work on symbolic links, or their targets.
    #
    followSymlinks: true


  Conditions:

    Condition: {}
